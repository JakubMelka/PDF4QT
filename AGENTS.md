# Codex Instructions

- Build the project after changing code and run the unit tests (`ctest` from the build directory, or the individual QtTest executables); a change is only complete when it compiles warning-free and the tests pass. Report build and test results.
- Preserve CRLF line endings when creating or editing source and text files.
